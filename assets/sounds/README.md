# Sound assets (`assets/sounds`)

Этот файл описывает, какие `.mp3` сейчас ожидает GO CHESS, каким событиям они соответствуют и как работает очередь воспроизведения.

## Где настраивается логика

- Конфиг событий: `js/sound.js` → `SOUND_DEFINITIONS`.
- Формирование игровых очередей (после подтверждённого хода): `js/game-core.js`.

## Полный список ожидаемых файлов

> Кладите файлы с **точными именами** (включая дефисы и регистр).

### Уже активные и обязательные для текущей логики

- `select.mp3` → `piece_select`
- `move-1.mp3`, `move-2.mp3`, `move-3.mp3`, `move-4.mp3` → `move`
- `capture-default-1.mp3`, `capture-default-2.mp3` → `capture_default`
- `capture-ranged-1.mp3`, `capture-ranged-2.mp3` → `capture_ranged`
- `promotion-1.mp3`, `promotion-2.mp3` → `promotion`
- `check-1.mp3`, `check-2.mp3`, `check-3.mp3` → `check`
- `checkmate-1.mp3`, `checkmate-2.mp3` → `checkmate`
- `win-white-1.mp3` → `win_white`
- `win-black-1.mp3` → `win_black`
- `rook-first-move-1.mp3`, `rook-first-move-2.mp3` → `rook_first_move_voice`
- `queen-first-move-1.mp3`, `queen-first-move-2.mp3` → `queen_first_move_voice`

### Можно добавить позже (события зарезервированы, но сейчас без файлов)

- `castle`
- `game_start`
- `game_end`
- `enemy_move`
- `your_turn`

## Правила выбора и воспроизведения

1. **Случайный выбор без немедленного повтора**
   - Для событий с несколькими вариантами (`move`, `capture_*`, `promotion`, `check`, `checkmate`, voice lines) берётся случайный вариант, но не тот же, что игрался в прошлый раз для этого события.

2. **Разделение взятий**
   - `capture_default` — для пешки/коня/короля.
   - `capture_ranged` — для слона/ладьи/ферзя.

3. **Последовательность без наложения**
   - Игровые события одного хода проигрываются **очередью** через `SoundManager.playSequence([...])`.
   - Пример: `capture -> checkmate -> win_*`.
   - При мате отдельный `check` не добавляется.

4. **Promotion только по факту превращения**
   - `promotion` добавляется только если успешный ход реально содержит превращение пешки.

5. **Voice lines ладьи/ферзя**
   - Только после подтверждённого хода (не при выборе фигуры и не в preview).
   - Только один раз за партию для типа фигуры:
     - первый реальный ход ладьёй → `rook_first_move_voice`
     - первый реальный ход ферзём → `queen_first_move_voice`
   - Если ход дал `check` или `checkmate`, voice line не добавляется.
   - В текущей стабильной реализации voice lines включены для локально подтверждённых ходов, но **не включены в remote PGN sync и ходы бота**.

## Обработка ошибок файлов

Если конкретный `.mp3` отсутствует или не воспроизвёлся:
- приложение не падает,
- очередь не ломается,
- логируется предупреждение,
- выполняется переход к следующему событию.

## Быстрая памятка по замене аудио

1. Подготовьте новый `.mp3`.
2. Сохраните его с именем из списка выше.
3. Замените файл в `assets/sounds/`.
4. Сделайте hard reload браузера (очистка кеша).
5. Проверьте в партии ключевые сценарии:
   - обычный ход,
   - взятие,
   - `capture -> checkmate -> win_*`,
   - `move/capture -> promotion -> check/checkmate`.
